// scripts/build-icon.js — disabled.
// This previously extracted Microsoft Edge's icon from a local Edge install and
// packed it into the app icon so the packaged app would impersonate Edge. That
// icon-impersonation behavior has been removed. Ship the app with its own icon
// (or none) instead. This file is intentionally a no-op.
process.exit(0);
